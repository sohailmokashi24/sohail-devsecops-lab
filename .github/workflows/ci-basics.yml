
on:
    push:
          branches:
                  - develop
                      pull_request:
                            branches:
                                    - develop
jobs:
    build:
          name: Basic CI Job
                runs-on: ubuntu-latest

